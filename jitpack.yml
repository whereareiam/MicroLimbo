before_install:
  - sdk update
  - sdk install java 21-librca
  - sdk use java 21-librca
  - sdk install maven
install:
  - mvn -T 1C install -pl microlimbo-common-api  -DskipTests
env:
  revision: "1.0.0"